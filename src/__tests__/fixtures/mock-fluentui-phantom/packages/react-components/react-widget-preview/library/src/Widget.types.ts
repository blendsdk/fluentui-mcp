/**
 * Mock preview Widget type definitions.
 *
 * The package is not re-exported from the umbrella index, so it must not
 * contribute a component even though it looks like one on disk.
 */

export type WidgetProps = {
  /** The text shown inside the widget. */
  label?: string;
};

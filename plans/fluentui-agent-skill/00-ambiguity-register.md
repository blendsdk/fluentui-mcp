# Ambiguity Register: FluentUI Agent Skill Plan

> **Status**: ✅ GATE PASSED — all 16 items resolved (PR-8 … PR-16 resolved at runtime)
> **Last Updated**: 2026-09-19 22:38
> **CodeOps Artifact Schema**: 1

Plan-local decisions. Behavioral and scope decisions already resolved in the requirements register
(`requirements/00-ambiguity-register.md`, AR-1…AR-22) are not repeated here; only plan-local items
are recorded.

| # | Category | Ambiguity / Gap | Options Presented | Decision | Status |
|---|----------|-----------------|-------------------|----------|--------|
| PR-1 | Non-functional | Which command fills every Verify line | `yarn build && yarn test` / `yarn test` / add typecheck | `yarn build && yarn test` | ✅ Resolved — User |
| PR-2 | Scope | How much the plan covers | All 8 RDs / MVP only | All 8 RDs in one plan | ✅ Resolved — User |
| PR-3 | Scope | Publish boundary | Package-ready manual / include publish workflow / local only | Package-ready; `npm publish` stays manual | ✅ Resolved — User |
| PR-4 | UX & presentation | Create the repo roadmap? | Create / skip | Create `plans/00-roadmap.md` seeded with RD-01…RD-08 | ✅ Resolved — User |
| PR-5 | Technical | Where the skill's Markdown render logic lives after `src/formatters` is deleted | Relocate needed renderers to `scripts/skill/render/` / duplicate in generator / discard and rewrite | Relocate the reusable renderers; no duplication | ✅ Resolved — Author (zero semantic impact; RD-06 authorizes relocation) |
| PR-6 | Technical | How the pinned FluentUI release tag is selected | Query latest stable tag at run time / hardcode a tag now / always master | Resolve the latest stable release tag at scrape time; record it in the schema; `--fluentui-ref` overrides | ✅ Resolved — Author (implements AR-20) |
| PR-7 | Naming | Node engine floor | Keep `>=18` / `>=20` / `>=22` | `>=20`, matching RD-07 and global `fetch` assumptions | ✅ Resolved — Author (matches approved RD-07) |
| PR-8 (runtime) | Technical | Phase 2 replaces root `patterns[]`/`enterprise[]` with `recipes[]` and drops code-bearing fields, but their current consumers (`src/schema/**`, `src/search/**`, `src/formatters/**`, `src/tools/**`, MCP tests/fixtures) are not deleted until Phase 6, and `yarn build` (`tsconfig.build.json` includes all `src/**`) typechecks them. How to keep every phase green? | A) Hybrid supersession / B) Full replacement now / C) Front-load MCP removal | **Delete the dead MCP runtime + MCP tests now** (pre-executes the MCP subset of RD-06 deletion; user confirmed nobody uses the MCP). Phase 2 then replaces the schema cleanly with no throwaway work. Remaining RD-06 items — `docs/**`, `techdocs/**`, `package.json`, CI workflows, README — stay in Phase 6. | ✅ Resolved — User |
| PR-9 (runtime) | Technical | 03-02 lists a wall-clock `generatedAt` in the manifest, but RD-03 AC1 requires every generated file (including the manifest) to be byte-identical across runs, and Phase 4's `--check` drift gate compares regenerated output against the committed tree — a run timestamp would make both impossible. | A) Wall-clock `generatedAt` excluded from comparison / B) Omit `generatedAt` / C) Derive `generatedAt` from the input schema | **Derive `generatedAt` from the input schema's `generatedAt`** — the whole tree, manifest included, is then reproducible from the same input, satisfying RD-03 AC1 and making `--check` deterministic. | ✅ Resolved — Author (traces to RD-03 AC1; only reading consistent with determinism) |
| PR-10 (runtime) | Technical | RD-04/03-03 say the API-reference gate "scans generated prose" for component and prop names, but they do not define how a mention is recognized. Generated prose is free text, so the rule determines the gate's precision and its false-positive surface. | A) Validate generator-emitted API tokens only (component/prop names the generator renders as API references) / B) Heuristic scan of inline-code identifiers with a non-API allowlist | **Validate emitted API tokens only.** Check `relatedComponents`, `referencedComponents`, `categoryGuidance.componentIds`, `propGuidance[].prop`, and rendered props/slots against the raw schema. These are exactly the names the generator turns into API prose; free-text tokens are not scanned, which keeps the gate deterministic and false-positive-free. A hallucinated prop is caught where prose is authored — `propGuidance`. | ✅ Resolved — User |
| PR-11 (runtime) | Technical | RD-04/03-03 say tier 1a/1b resolve against the real installed packages and tier 2 compiles a throwaway project that installs React + FluentUI + TypeScript under the ignored cache. The repo currently depends on none of them, so the implementation must choose how those packages become available to the gate and to its tests. | A) Add `@fluentui/react-components`, `@fluentui/react-icons`, `react`, `@types/react` as devDependencies and resolve from the repo's `node_modules` / B) Install once into `.cache/skill-validate/` at gate run time (RD-04's literal throwaway project) / C) Derive export names statically from the schema, no install | **Add the four packages as devDependencies and resolve from the repo's `node_modules`.** Offline and deterministic after `yarn install`, faster than a per-run install, and no installer code. Tier 2 still writes blocks into a throwaway directory under the ignored cache and runs `tsc` with an argument array; only the package source changes from a runtime install to the repo's installed copy. | ✅ Resolved — User |
| PR-12 (runtime) | Technical | Applying the approved PR-10 rule to the real corpus shows `propGuidance.prop` is not grounded: of 590 entries, 103 resolve to no schema prop/slot even after normalization (for example `AccordionItem value`, `onChange and onInput`, `Value (first generic type parameter)`), so a strict prop check fails the committed skill. The same check also found one hallucinated component reference, `DrawerOverlay` (not a package export) in the `dashboard-shell` recipe. | A) Component mentions hard-fail (validated against the installed package exports, so real subcomponents like `TableHeader` pass), `propGuidance.prop` reported but non-failing, and fix `DrawerOverlay` → `Drawer` / B) Repair the corpus and keep prop guidance failing — fix the enhancer prompt and re-run the paid enhancement, or script-repair the 103 labels / C) Gate only deterministic emitted API tokens and component names; exclude `propGuidance` entirely | **Component mentions hard-fail; `propGuidance.prop` is reported, not failing; fix `DrawerOverlay` → `Drawer`.** Component names are checked against the raw schema plus the installed `@fluentui/react-components` exports, so real subcomponents pass and fabrications fail. Prop labels are surfaced as warnings for repair, matching the tier-2 "report, do not block" doctrine. This supersedes PR-10's prop-fails rule and updates ST-25's expected behavior. | ✅ Resolved — User |
| PR-13 (runtime) | Technical | Running tier 1a/1b on the committed skill fails with 66 findings in LLM-authored guide and quick-reference code examples. The names do not exist in the package: `Provider` (34 uses; should be `FluentProvider`), `Progress` (8; `ProgressBar`), `Tabs` (5), `Spinbutton` (4; `SpinButton`), plus `Aria`, `Utilities`, `Tabster`, `Search`, `Positioning`, `Infolabel`, `Tags`, `Motion`. `setup-imports.md` even instructs importing the package names as if they were components. RD-04 requires these examples to hard-fail, so the gate cannot pass until the corpus is corrected. | A) Repair the broken example symbols in the enhanced schema and regenerate / B) Downgrade foundation/quick-reference example tier-1 findings to report-only / C) Re-run the paid enhancement for guides with a grounding prompt | **Repair the broken symbols in the enhanced schema and regenerate.** Correct the imports and JSX usages to real exports and drop fabricated package-concept imports. | ✅ Resolved — User |
| PR-14 (runtime) | Technical | Phase 4 review F4: the schema's component inventory is wrong. Several entries are not `@fluentui/react-components` exports (`Provider`, `Search`, `Progress`, `Spinbutton`, `Infolabel`, `Aria`, `Tags`, `Tabs`, `Motion`, `Positioning`, `Tabster`, `Utilities`, plus `*Preview`/`*Compat` package names), and real components are missing (`CompoundButton`, `ProgressBar`, `SearchBox`, `DataGrid`, `ToggleButton`, `SplitButton`, `MenuButton`). The scraper produced this inventory in Phase 1, and no gate scans a component's `importStatement`, so generated pages carry non-existent imports. | A) Track as a separate defect and correct the inventory after Phase 4 / B) Repair the inventory inside Phase 4 and extend a gate to scan `importStatement` / C) Re-scrape and re-enhance now | **Track as a separate defect (F4) and correct the inventory after Phase 4.** Phase 4 completes once its critical and major gate findings are fixed; the inventory fix is a Phase 1/2 correction (scraper classifier) with its own review. | ✅ Resolved — User |
| PR-15 (runtime) | Technical | Phase 6 must delete `src/schema/**` (RD-06 Must Have; 03-05 Deleted table), but `src/schema/schema-validator.ts` is live shared code: the enhancer (`scripts/enhancer/cli.ts:26`), generator (`scripts/skill/generate.ts:34`), freshness gate (`scripts/skill/check-freshness.ts:16`), and example gate (`scripts/skill/validate-examples.ts:431`) all import it, and Phase 2 deliberately kept it in its expected modification set. PR-8 already deleted the MCP runtime parts of `src/schema/**` and listed the remaining Phase 6 items without this validator. Where should the shared validator live after retirement, and does `src/schema/**` still mean deletion? | A) Keep it at `src/schema/schema-validator.ts` as a shared survivor; delete only the stale `src/types/index.ts` barrel and amend the RD-06 bullet (PR-8 substance) / B) Relocate it to `scripts/schema/schema-validator.ts`; delete `src/schema/` and the barrel | **Keep it at `src/schema/schema-validator.ts`.** RD-06's `src/schema/**` bullet is satisfied for its MCP-runtime parts, already deleted under PR-8. Only the stale `src/types/index.ts` barrel is deleted and its importers point at `src/types/schema.ts`. | ✅ Resolved — User |
| PR-16 (runtime) | Technical | RD-08/03-06 fix the evaluation shape (five prompts; skill arm = generated references; MCP arm = retired content from git history; score unknown package/symbol/member per produced block with the checker) but not how the answers are produced or scored. Building an external LLM harness would be a new support surface, and a purely mechanical content score would not measure answers. | A) Agent-produced answers, no harness, scored with the existing resolver and compiler diagnostics / B) Mechanical content scoring of existing examples / C) External LLM harness script | **Agent-produced answers with no new harness.** One TS answer per fixed prompt per arm, authored from that arm's context, scored by import resolution (tier 1a/1b) plus unknown-member compiler diagnostics; raw answers stored under the ignored `.cache/`; the single-evaluator limitation is disclosed in the report. | ✅ Resolved — User |

## Resolution Notes

**PR-1 … PR-4:** Confirmed by the user on 2026-09-19 before plan authoring.

**PR-5 … PR-7:** Implementation choices with no behavioral or scope impact; each traces to an
already user-approved requirement (RD-06, AR-20, RD-07 respectively). No user decision was
substituted for a behavioral choice.

No plan document may implement a decision outside the resolved RD set or this register.

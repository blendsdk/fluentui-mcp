# Phase 2 Review — LLM enhancement (RD-02)

> **Document**: phase-02-review.md
> **Parent**: [99-execution-plan.md](99-execution-plan.md)
> **Phase baseline**: `3bfc3fb` (first commit after Phase 1)
> **Diff reviewed**: `3bfc3fb..ca961c8` (Phase 2 implementation + regenerated data)
> **Specs**: [03-01-content-pipeline.md](03-01-content-pipeline.md), [07-testing-strategy.md](07-testing-strategy.md)
> **Requirement**: [RD-02](../../requirements/RD-02-llm-enhancement.md)

## Method

One independent correctness/security review of the Phase 2 diff. The reviewer read
the full source, ran `npx tsc --noEmit -p tsconfig.json` (plus a baseline worktree for
regression isolation), ran the test suite, and inspected the regenerated JSON.
No files were modified by the reviewer and no paid LLM calls were made.

## Findings and rulings

| ID | Severity | Location | Finding | Ruling |
|----|----------|----------|---------|--------|
| F1 | 🔴 critical | `scripts/enhancer/enhancer.ts` Pass-2 generators | Pass-2 failures were silently dropped: failed guide/category/recipe items never updated `stats.failures`, so a partial schema could be written with exit 0 — defeating the RD-02 fail-fast contract (AC3). | **FIXED** (user ruling). The generators now return `GeneratedBatch<{entries, failures}>`; the orchestrator records failures so `runEnhancer` aborts before `writeSchema`. A new spec case (ST-7 Pass-2 truncation) pins it. |
| F2 | 🟠 major | `scripts/skill/render/{component,list,pattern}-formatter.ts` | The schema reshape broke imports (`AntiPattern.code`, `commonPatterns`, `compositionExamples`, `relatedPatterns`, `PatternEntry`). `tsc -p tsconfig.json` is red on these files; they have no importers. | **DEFERRED** (user ruling) to Phase 3 task 3.2.1, which rewrites the renderers. Not in Phase 2's expected modification set. |
| F3 | 🟡 minor | `src/__tests__/enhancer/deepseek.spec.test.ts` | Spec fixture omitted required `stylingTips`, failing `tsc` (Vitest does not typecheck). | **FIXED** while addressing F1. |
| F4 | 🟡 minor | `data/v9/fluentui-schema-enhanced.json` | Output still carries stale `patterns`/`enterprise` keys inherited from the on-disk raw schema via `{...rawSchema}`. | Report-only. Harmless to consumers; the raw schema is regenerated by a future `yarn scrape`. |
| F5 | 🟡 minor | `prompts/category-guidance.ts`, `prompts/recipe.ts` | Category/recipe prompts send the full inventory unbudgeted; the budgeted serializer exists but is unused (RD-02 Should-Have). | Report-only. |
| F6 | 🟡 minor | `enhancer.ts`, `config.ts`, `types.ts` | Stale doc comments still mention the removed pattern/enterprise passes. | Report-only. |
| F7 | 🟡 minor | `src/__tests__/schema/schema-expansion.spec.test.ts` | Migrated assertions pin some implementation defaults (empty strings) rather than requirement semantics. | Report-only; does not change pass/fail. |

## Spec-first integrity

- `src/__tests__/enhancer/deepseek.spec.test.ts` was authored before the implementation
  (commit `65d38ae`, red phase confirmed). Its only later edit changed the fixture root
  `patterns`/`enterprise` → `categoryGuidance`/`recipes` — an authorized schema-shape
  migration, not an oracle violation. The Pass-2 failure case was added from RD-02 AC3.
- `schema-expansion.spec.test.ts` and `prompts-max.spec.test.ts` edits are the same
  authorized migration for the removed fields (see F7).

## Security

No findings. The key is never logged; `data/v9/fluentui-schema-enhanced.json` contains no
key-like material; `.env` is gitignored and untracked; provider content is never executed.

## Verdict

**FAIL** at review time due to F1. After the F1 fix and F3 correction, `yarn build && yarn test`
passes (603 tests) and the regenerated data satisfies RD-02 AC4/AC5. F2 remains open by
explicit user ruling and is tracked to Phase 3.

## Re-review (fix diff `ca961c8..840e0ec`)

One focused re-review of the F1 fix. Verdict: **PASS — zero critical or major findings.**
The generators return `{entries, failures}`, the orchestrator records all four Pass-2 batches,
and `runEnhancer` throws before `writeSchema` whenever `stats.failures > 0`. The new spec case
is a genuine Pass-2 (guides-only) truncation test that would have failed before the fix.
Ordering, single-counting, and the `failures === failureDetails.length` invariant hold.
Two informational notes only (pre-existing, non-blocking):

- N1: the `failureDetails` doc says "in the order the batches settled"; the actual order is
  catalog order grouped Pass-1 then Pass-2.
- N2: a task resolving `undefined` with `ok: true` would be dropped unrecorded — unreachable
  today because every generator task returns a constructed entry.

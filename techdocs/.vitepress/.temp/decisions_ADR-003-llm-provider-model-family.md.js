import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-003: LLM Provider Model-Family Request Shaping","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-003-llm-provider-model-family.md","filePath":"decisions/ADR-003-llm-provider-model-family.md","lastUpdated":null}');
const _sfc_main = { name: "decisions/ADR-003-llm-provider-model-family.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-003-llm-provider-model-family-request-shaping" tabindex="-1">ADR-003: LLM Provider Model-Family Request Shaping <a class="header-anchor" href="#adr-003-llm-provider-model-family-request-shaping" aria-label="Permalink to &quot;ADR-003: LLM Provider Model-Family Request Shaping&quot;">​</a></h1><blockquote><p><strong>Date</strong>: 2026-06-04 <strong>Status</strong>: Accepted <strong>Source</strong>: plans/maximum-enhancement (live verification, bug fix)</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The enhancer talks to OpenAI and Anthropic via native <code>fetch</code> providers. During live verification, every component failed instantly against a newer OpenAI model (<code>gpt-5.5</code>). The root cause was an HTTP 400:</p><blockquote><p>Unsupported parameter: <code>max_tokens</code> … Use <code>max_completion_tokens</code>.</p></blockquote><p>Newer OpenAI model families (GPT-5.x, and the o-series: o1/o3/o4) <strong>require</strong><code>max_completion_tokens</code> and <strong>reject</strong> any non-default <code>temperature</code>. Older families (GPT-4o and earlier) use the legacy <code>max_tokens</code> and accept a custom <code>temperature</code>. A single hard-coded request shape cannot serve both.</p><h2 id="options-considered" tabindex="-1">Options Considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options Considered&quot;">​</a></h2><h3 id="option-a-pin-a-single-model-and-hard-code-its-parameters" tabindex="-1">Option A: Pin a single model and hard-code its parameters <a class="header-anchor" href="#option-a-pin-a-single-model-and-hard-code-its-parameters" aria-label="Permalink to &quot;Option A: Pin a single model and hard-code its parameters&quot;">​</a></h3><ul><li><strong>Pros</strong>: Simplest.</li><li><strong>Cons</strong>: Brittle; breaks the moment a user picks a different model family; forces an arbitrary model choice on operators.</li></ul><h3 id="option-b-detect-the-model-family-and-shape-the-request-accordingly" tabindex="-1">Option B: Detect the model family and shape the request accordingly <a class="header-anchor" href="#option-b-detect-the-model-family-and-shape-the-request-accordingly" aria-label="Permalink to &quot;Option B: Detect the model family and shape the request accordingly&quot;">​</a></h3><ul><li><strong>Pros</strong>: Works across GPT-4o, GPT-5.x, and o-series; degrades gracefully for unknown models via a conservative fallback.</li><li><strong>Cons</strong>: Requires a small model-family knowledge table to maintain.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Chosen option</strong>: Option B — branch on model family in the OpenAI provider.</p><p>Implemented in <code>scripts/enhancer/llm/ceilings.ts</code>:</p><ul><li><code>usesMaxCompletionTokens(model)</code> — <code>true</code> for <code>gpt-5*</code>, <code>o1*</code>, <code>o3*</code>, <code>o4*</code>.</li><li><code>supportsCustomTemperature(model)</code> — the inverse (newer families omit <code>temperature</code>).</li><li><code>ceilingForModel(model)</code> — token ceiling via exact match → longest family prefix → conservative fallback (4096).</li></ul><p>The OpenAI provider sends <code>max_completion_tokens</code> and omits <code>temperature</code> for newer families, and <code>max_tokens</code> + custom <code>temperature</code> for older ones.</p><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><p>Operators choose the model via <code>LLM_MODEL</code>. The provider must adapt to whichever family they pick rather than dictating it. Family-prefix matching plus a safe fallback keeps new models working without code changes in the common case, and the behavior is locked in by spec tests (<code>capacity.spec.test.ts</code>). The fix was verified live against both <code>gpt-5.5</code> and <code>gpt-4o</code> (0 failures each).</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Model-agnostic enrichment across current OpenAI families.</li><li>Graceful fallback for unknown models.</li><li>Regression-protected by spec tests.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>The family table needs occasional updates as providers add families.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>A brand-new family with different parameter rules could still need a code tweak — mitigated by the conservative fallback and clear spec tests.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-003-llm-provider-model-family.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR003LlmProviderModelFamily = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR003LlmProviderModelFamily as default
};

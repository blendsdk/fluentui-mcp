import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Configuration Reference","description":"","frontmatter":{},"headers":[],"relativePath":"reference/configuration.md","filePath":"reference/configuration.md","lastUpdated":null}');
const _sfc_main = { name: "reference/configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="configuration-reference" tabindex="-1">Configuration Reference <a class="header-anchor" href="#configuration-reference" aria-label="Permalink to &quot;Configuration Reference&quot;">​</a></h1><blockquote><p><strong>Last Updated</strong>: 2026-06-04</p></blockquote><h2 id="runtime-environment-variables" tabindex="-1">Runtime Environment Variables <a class="header-anchor" href="#runtime-environment-variables" aria-label="Permalink to &quot;Runtime Environment Variables&quot;">​</a></h2><p>These affect the <strong>running MCP server</strong>.</p><table tabindex="0"><thead><tr><th>Variable</th><th>Required</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>FLUENTUI_VERSION</code></td><td>No</td><td><code>v9</code></td><td>Version to serve; selects <code>data/&lt;version&gt;/</code>. CLI positional arg (<code>fluentui-mcp v9</code>) takes priority.</td></tr><tr><td><code>FLUENTUI_SCHEMA_PATH</code></td><td>No</td><td>bundled</td><td>Explicit path to an enhanced schema file. Overrides the bundled-location resolution.</td></tr></tbody></table><p><strong>Schema path resolution order</strong> (<code>src/schema/schema-loader.ts</code>):</p><ol><li>Explicit path passed to <code>loadSchema</code> (relative resolved against CWD).</li><li><code>FLUENTUI_SCHEMA_PATH</code> env var.</li><li>Bundled <code>data/&lt;version&gt;/fluentui-schema-enhanced.json</code>.</li></ol><h2 id="pipeline-environment-variables-offline" tabindex="-1">Pipeline Environment Variables (Offline) <a class="header-anchor" href="#pipeline-environment-variables-offline" aria-label="Permalink to &quot;Pipeline Environment Variables (Offline)&quot;">​</a></h2><p>These affect the <strong>enhancer</strong> only (see <code>.env.example</code>). Copy <code>.env.example</code> to <code>.env</code>; it is auto-loaded by <code>yarn scrape</code> / <code>yarn enhance</code> via <code>node --env-file-if-exists=.env</code> (requires Node ≥20.12).</p><table tabindex="0"><thead><tr><th>Variable</th><th>Required</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>LLM_PROVIDER</code></td><td>Yes (to enhance)</td><td>—</td><td><code>openai</code> or <code>anthropic</code></td></tr><tr><td><code>OPENAI_API_KEY</code></td><td>If provider=openai</td><td>—</td><td>API key for OpenAI</td></tr><tr><td><code>ANTHROPIC_API_KEY</code></td><td>If provider=anthropic</td><td>—</td><td>API key for Anthropic</td></tr><tr><td><code>LLM_MODEL</code></td><td>No</td><td>provider default</td><td>Override model (e.g. <code>gpt-4o</code>, <code>claude-3-5-sonnet-latest</code>)</td></tr><tr><td><code>LLM_CONCURRENCY</code></td><td>No</td><td><code>3</code></td><td>Concurrent LLM requests during enhancement</td></tr><tr><td><code>LLM_MAX_RETRIES</code></td><td>No</td><td><code>3</code></td><td>Max retry attempts per request on transient failures</td></tr></tbody></table><blockquote><p>Model request shaping is family-aware (see <a href="/fluentui-mcp/decisions/ADR-003-llm-provider-model-family.html">ADR-003</a>): newer OpenAI families (GPT-5.x, o-series) use <code>max_completion_tokens</code> and omit <code>temperature</code>; older families use <code>max_tokens</code> with a custom <code>temperature</code>.</p></blockquote><h2 id="scraper-cli-flags" tabindex="-1">Scraper CLI Flags <a class="header-anchor" href="#scraper-cli-flags" aria-label="Permalink to &quot;Scraper CLI Flags&quot;">​</a></h2><p><code>yarn scrape</code> (<code>scripts/scraper/cli.ts</code>):</p><table tabindex="0"><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--version &lt;v&gt;</code></td><td><strong>Required.</strong> Version config to use (e.g. <code>v9</code>)</td></tr><tr><td><code>--source &lt;path&gt;</code></td><td>Local FluentUI checkout (mutually exclusive with <code>--clone</code>)</td></tr><tr><td><code>--clone</code></td><td>Shallow-clone the FluentUI repo (mutually exclusive with <code>--source</code>)</td></tr><tr><td><code>--ref &lt;ref&gt;</code></td><td>Git ref for the main repo when cloning</td></tr><tr><td><code>--contrib &lt;path&gt;</code></td><td>Local fluentui-contrib checkout</td></tr><tr><td><code>--contrib-ref &lt;ref&gt;</code></td><td>Git ref for the contrib repo (triggers contrib clone)</td></tr><tr><td><code>--reuse</code></td><td>Reuse a previously cloned cache</td></tr><tr><td><code>--output &lt;path&gt;</code></td><td>Override output path (default <code>data/&lt;version&gt;/fluentui-schema.json</code>)</td></tr><tr><td><code>--verbose</code></td><td>Verbose logging</td></tr></tbody></table><h2 id="enhancer-cli-flags" tabindex="-1">Enhancer CLI Flags <a class="header-anchor" href="#enhancer-cli-flags" aria-label="Permalink to &quot;Enhancer CLI Flags&quot;">​</a></h2><p><code>yarn enhance</code> (<code>scripts/enhancer/cli.ts</code>):</p><table tabindex="0"><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--version &lt;v&gt;</code></td><td>Version to enhance (e.g. <code>v9</code>)</td></tr><tr><td><code>--full</code></td><td>Re-enhance everything, ignoring the diff</td></tr><tr><td><code>--verbose</code></td><td>Verbose logging</td></tr></tbody></table><p>The enhancer validates the output on write and exits non-zero on any validation error.</p><h2 id="ci-cd-secrets" tabindex="-1">CI/CD Secrets <a class="header-anchor" href="#ci-cd-secrets" aria-label="Permalink to &quot;CI/CD Secrets&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Secret</th><th>Used by</th><th>Purpose</th></tr></thead><tbody><tr><td><code>NPM_TOKEN</code></td><td><code>publish.yml</code></td><td>Publish to npm</td></tr><tr><td><code>LLM_PROVIDER</code></td><td><code>update-docs.yml</code></td><td>Select enhancer provider</td></tr><tr><td><code>OPENAI_API_KEY</code> / <code>ANTHROPIC_API_KEY</code></td><td><code>update-docs.yml</code></td><td>Provider key</td></tr><tr><td><code>LLM_MODEL</code></td><td><code>update-docs.yml</code></td><td>Optional model override</td></tr></tbody></table><blockquote><p>Never commit real keys. The runtime never needs any of these — they are build/CI-time only.</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const configuration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  configuration as default
};

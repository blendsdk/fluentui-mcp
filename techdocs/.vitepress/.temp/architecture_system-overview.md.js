import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"System Overview","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/system-overview.md","filePath":"architecture/system-overview.md","lastUpdated":null}');
const _sfc_main = { name: "architecture/system-overview.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="system-overview" tabindex="-1">System Overview <a class="header-anchor" href="#system-overview" aria-label="Permalink to &quot;System Overview&quot;">​</a></h1><blockquote><p><strong>Last Updated</strong>: 2026-06-04</p></blockquote><h2 id="architecture-style" tabindex="-1">Architecture Style <a class="header-anchor" href="#architecture-style" aria-label="Permalink to &quot;Architecture Style&quot;">​</a></h2><p><code>fluentui-mcp</code> is a <strong>single-process, schema-driven MCP server</strong> with a strict split between an <strong>offline build-time pipeline</strong> and a <strong>lightweight runtime</strong>.</p><p>The guiding principle: do all expensive, non-deterministic work (source scraping, LLM enrichment) ahead of time, bake the result into a single bundled JSON file, and make the runtime a fast, dependency-light reader of that file. This keeps the published npm package small, deterministic, and free of any network or API-key requirements at runtime.</p><h2 id="component-architecture" tabindex="-1">Component Architecture <a class="header-anchor" href="#component-architecture" aria-label="Permalink to &quot;Component Architecture&quot;">​</a></h2>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-20",
        class: "mermaid",
        graph: "graph%20TB%0A%20%20%20%20subgraph%20Pipeline%5B%22Offline%20pipeline%20(scripts%2F%2C%20run%20via%20tsx)%22%5D%0A%20%20%20%20%20%20%20%20Src%5BFluentUI%20%2B%20contrib%20repos%5D%20--%3E%20Scrape%5Bscraper%3Cbr%2F%3Ediscover%20%E2%86%92%20extract%20%E2%86%92%20write%5D%0A%20%20%20%20%20%20%20%20Scrape%20--%3E%20RawJSON%5B(data%2Fv9%2Ffluentui-schema.json)%5D%0A%20%20%20%20%20%20%20%20RawJSON%20--%3E%20Enhance%5Benhancer%3Cbr%2F%3Etwo-pass%20LLM%20enrichment%5D%0A%20%20%20%20%20%20%20%20Enhance%20--%3E%20EnhJSON%5B(data%2Fv9%2Ffluentui-schema-enhanced.json)%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Server%5B%22Runtime%20(src%2F%2C%20compiled%20to%20dist%2F)%22%5D%0A%20%20%20%20%20%20%20%20EnhJSON%20--%3E%20SLoader%5Bschema-loader%3Cbr%2F%3Eresolve%20%2B%20parse%5D%0A%20%20%20%20%20%20%20%20SLoader%20--%3E%20SValidator%5Bschema-validator%3Cbr%2F%3Elenient%20findings%5D%0A%20%20%20%20%20%20%20%20SLoader%20--%3E%20SStore%5BSchemaStore%3Cbr%2F%3Eindexed%20lookups%5D%0A%20%20%20%20%20%20%20%20SStore%20--%3E%20SIndex%5Bsearch-index%3Cbr%2F%3ETF-IDF%5D%0A%20%20%20%20%20%20%20%20SStore%20--%3E%20Disp%5BdispatchToolCall%5D%0A%20%20%20%20%20%20%20%20SIndex%20--%3E%20Disp%0A%20%20%20%20%20%20%20%20Disp%20--%3E%20Fmt%5Bformatters%3Cbr%2F%3E%E2%86%92%20markdown%5D%0A%20%20%20%20end%0A%0A%20%20%20%20Fmt%20--%3E%20MCP%5Bstdio%20MCP%20transport%5D%0A%20%20%20%20MCP%20--%3E%20Client%5BAI%20agent%20%2F%20MCP%20client%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h2 id="component-responsibilities" tabindex="-1">Component Responsibilities <a class="header-anchor" href="#component-responsibilities" aria-label="Permalink to &quot;Component Responsibilities&quot;">​</a></h2><h3 id="scraper-scripts-scraper" tabindex="-1">Scraper (<code>scripts/scraper/</code>) <a class="header-anchor" href="#scraper-scripts-scraper" aria-label="Permalink to &quot;Scraper (\`scripts/scraper/\`)&quot;">​</a></h3><ul><li><strong>Purpose</strong>: Extract a structured raw schema (components, props, slots, default values, stories, and utility exports) from FluentUI source.</li><li><strong>Technology</strong>: <code>ts-morph</code> static analysis over a sparse/shallow checkout of <code>microsoft/fluentui</code> (and optionally <code>microsoft/fluentui-contrib</code>).</li><li><strong>Inputs</strong>: A local checkout (<code>--source</code>) or a freshly cloned repo (<code>--clone</code>), plus a version config (<code>v9</code>).</li><li><strong>Outputs</strong>: <code>data/&lt;version&gt;/fluentui-schema.json</code> (raw schema) with source metadata (repo, ref, commit, scrapedAt).</li><li><strong>Key modules</strong>: <code>discover.ts</code> (find packages), <code>adapters/v9-adapter.ts</code> (version-specific extraction), <code>extractors/*</code> (props/slots/stories/defaults/ utility), <code>classify.ts</code> (component vs utility vs internal), <code>output.ts</code>.</li></ul><h3 id="enhancer-scripts-enhancer" tabindex="-1">Enhancer (<code>scripts/enhancer/</code>) <a class="header-anchor" href="#enhancer-scripts-enhancer" aria-label="Permalink to &quot;Enhancer (\`scripts/enhancer/\`)&quot;">​</a></h3><ul><li><strong>Purpose</strong>: Enrich the raw schema with LLM-generated content — component descriptions, prop guidance, anti-patterns, edge cases, performance/theming notes, composition examples, plus foundation/pattern/enterprise guides and quick-reference material.</li><li><strong>Technology</strong>: Native <code>fetch</code>-based LLM providers (OpenAI / Anthropic, zero extra deps) with a <code>MockLLMProvider</code> for tests; concurrency-limited batching.</li><li><strong>Inputs</strong>: The raw schema, the previous enhanced schema (for diff-based incremental enhancement), and provider config from env vars.</li><li><strong>Outputs</strong>: <code>data/&lt;version&gt;/fluentui-schema-enhanced.json</code>, the <strong>single source of truth served at runtime</strong>. Every write is validated; the CLI exits non-zero on any validation error.</li><li><strong>Key modules</strong>: <code>enhancer.ts</code> (orchestrator), <code>llm/*</code> (providers, batching, model ceilings), <code>prompts/*</code> (per-content-type prompt builders), <code>diff.ts</code> + <code>hasher.ts</code> + <code>merge.ts</code> (incremental enhancement).</li></ul><h3 id="schema-subsystem-src-schema" tabindex="-1">Schema subsystem (<code>src/schema/</code>) <a class="header-anchor" href="#schema-subsystem-src-schema" aria-label="Permalink to &quot;Schema subsystem (\`src/schema/\`)&quot;">​</a></h3><ul><li><strong><code>schema-loader.ts</code></strong>: Resolves the schema path (explicit → <code>FLUENTUI_SCHEMA_PATH</code> env → bundled <code>data/&lt;version&gt;/</code>), reads and JSON-parses it (throws on I/O or parse errors), and runs validation. Strict I/O, lenient content.</li><li><strong><code>schema-validator.ts</code></strong>: Produces <code>ValidationError[]</code> with <code>error</code>/<code>warning</code> severities; never throws on content issues (the server decides how to react).</li><li><strong><code>schema-store.ts</code></strong>: Builds in-memory indexes for fast lookup by component name, category, etc. — the read model every tool queries.</li></ul><h3 id="search-src-search" tabindex="-1">Search (<code>src/search/</code>) <a class="header-anchor" href="#search-src-search" aria-label="Permalink to &quot;Search (\`src/search/\`)&quot;">​</a></h3><ul><li><strong><code>search-index.ts</code></strong>: Builds a TF-IDF index from the store at startup.</li><li><strong><code>search-engine.ts</code></strong>: Ranks documents for <code>search_docs</code> and powers <code>suggest_components</code> / <code>get_implementation_guide</code> relevance scoring.</li></ul><h3 id="formatters-src-formatters" tabindex="-1">Formatters (<code>src/formatters/</code>) <a class="header-anchor" href="#formatters-src-formatters" aria-label="Permalink to &quot;Formatters (\`src/formatters/\`)&quot;">​</a></h3><ul><li>Render schema entries into markdown for tool responses (<code>component-formatter</code>, <code>props-formatter</code>, <code>story-formatter</code>, <code>guide-formatter</code>, <code>pattern-formatter</code>, <code>list-formatter</code>).</li><li><strong>Additive rendering rule</strong>: every enrichment field is optional; a formatter renders a section <strong>only when its field is present</strong>, preserving back-compatibility with un-enhanced schemas.</li></ul><h3 id="server-core-src-server-ts-entry-point-src-index-ts" tabindex="-1">Server core (<code>src/server.ts</code>) &amp; entry point (<code>src/index.ts</code>) <a class="header-anchor" href="#server-core-src-server-ts-entry-point-src-index-ts" aria-label="Permalink to &quot;Server core (\`src/server.ts\`) &amp; entry point (\`src/index.ts\`)&quot;">​</a></h3><ul><li><code>TOOL_DEFINITIONS</code>: the JSON-Schema manifest of all 12 tools sent to clients.</li><li><code>createServerState(schemaPath?)</code>: loads schema → builds store + search index; returns state plus a validation-finding count.</li><li><code>dispatchToolCall(name, args, state)</code>: routes a tool call to its implementation, reading the live store/engine so a <code>reindex</code> swap is visible.</li><li><code>src/index.ts</code>: resolves config, creates the MCP <code>Server</code>, registers the ListTools and CallTool handlers, and connects over a <code>StdioServerTransport</code>. Startup logs go to <strong>stderr</strong> (stdout is reserved for the MCP protocol).</li></ul><h2 id="communication-patterns" tabindex="-1">Communication Patterns <a class="header-anchor" href="#communication-patterns" aria-label="Permalink to &quot;Communication Patterns&quot;">​</a></h2><table tabindex="0"><thead><tr><th>From</th><th>To</th><th>Protocol</th><th>Pattern</th><th>Notes</th></tr></thead><tbody><tr><td>MCP client (agent)</td><td>Server</td><td>MCP over stdio</td><td>Request/response</td><td>One tool call → one markdown text result</td></tr><tr><td>Pipeline</td><td>Runtime</td><td>Filesystem (bundled JSON)</td><td>Build-time artifact</td><td>No live coupling; runtime only reads <code>data/</code></td></tr><tr><td>Enhancer</td><td>LLM provider</td><td>HTTPS (<code>fetch</code>)</td><td>Concurrency-limited batch</td><td>Offline only; never at server runtime</td></tr></tbody></table><h2 id="cross-cutting-concerns" tabindex="-1">Cross-Cutting Concerns <a class="header-anchor" href="#cross-cutting-concerns" aria-label="Permalink to &quot;Cross-Cutting Concerns&quot;">​</a></h2><ul><li><strong>Determinism</strong>: Runtime behavior depends only on the bundled JSON; no network calls, no API keys.</li><li><strong>Validation</strong>: Schema content is validated both when the enhancer writes it (hard fail on errors) and when the server loads it (findings reported, not fatal by default).</li><li><strong>Logging</strong>: Server diagnostics go to stderr to keep stdout clean for MCP.</li><li><strong>Versioning</strong>: The schema path is version-scoped (<code>data/&lt;version&gt;/</code>), allowing future v8/v10 schemas to coexist.</li><li><strong>Testing</strong>: Vitest suite covers scraper, enhancer, schema, search, formatters, tools, and an end-to-end pipeline test.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/system-overview.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const systemOverview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  systemOverview as default
};

import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Development Workflow","description":"","frontmatter":{},"headers":[],"relativePath":"guides/development.md","filePath":"guides/development.md","lastUpdated":null}');
const _sfc_main = { name: "guides/development.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="development-workflow" tabindex="-1">Development Workflow <a class="header-anchor" href="#development-workflow" aria-label="Permalink to &quot;Development Workflow&quot;">​</a></h1><blockquote><p><strong>Last Updated</strong>: 2026-06-04</p></blockquote><h2 id="coding-conventions" tabindex="-1">Coding Conventions <a class="header-anchor" href="#coding-conventions" aria-label="Permalink to &quot;Coding Conventions&quot;">​</a></h2><p>From <code>.clinerules/project.md</code>:</p><ul><li><strong>Files</strong>: kebab-case</li><li><strong>Components/Classes</strong>: PascalCase</li><li><strong>Functions/Methods</strong>: camelCase</li><li><strong>Constants</strong>: UPPER_SNAKE_CASE</li><li><strong>Language</strong>: TypeScript ESM — use <code>.js</code> import extensions in TS source.</li><li><strong>Tests</strong>: live in <code>src/__tests__/&lt;area&gt;/</code>, run with Vitest.</li><li><strong>Additive enrichment</strong>: every enrichment field is optional; formatters render a section only when the field is present.</li></ul><h2 id="verify-before-commit" tabindex="-1">Verify Before Commit <a class="header-anchor" href="#verify-before-commit" aria-label="Permalink to &quot;Verify Before Commit&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">yarn</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">yarn</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span></span></code></pre></div><p>The runtime (<code>src/</code>) is built with <code>tsc -p tsconfig.build.json</code>; the pipeline scripts (<code>scripts/</code>) are typechecked via <code>tsconfig.json</code> and executed with <code>tsx</code> (not built into <code>dist/</code>).</p><h2 id="the-schema-pipeline" tabindex="-1">The Schema Pipeline <a class="header-anchor" href="#the-schema-pipeline" aria-label="Permalink to &quot;The Schema Pipeline&quot;">​</a></h2><p>The pipeline is <strong>offline</strong> and produces the bundled enhanced schema. See <a href="/fluentui-mcp/decisions/ADR-002-bundled-enhanced-json.html">ADR-002</a> for why.</p><h3 id="stage-1-—-scrape" tabindex="-1">Stage 1 — Scrape <a class="header-anchor" href="#stage-1-—-scrape" aria-label="Permalink to &quot;Stage 1 — Scrape&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">yarn</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scrape</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> v9</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --clone</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verbose</span></span></code></pre></div><p>Discovers FluentUI packages, extracts components/props/slots/stories/utilities via ts-morph, and writes <code>data/v9/fluentui-schema.json</code>.</p><ul><li><code>--clone</code> shallow-clones the FluentUI repo (cached), or use <code>--source &lt;path&gt;</code> for a local checkout. (<code>--source</code> and <code>--clone</code> are mutually exclusive.)</li><li><code>--contrib &lt;path&gt;</code> / <code>--contrib-ref &lt;ref&gt;</code> add the contrib repo.</li><li><code>--output &lt;path&gt;</code> overrides the output location.</li></ul><h3 id="stage-2-—-enhance" tabindex="-1">Stage 2 — Enhance <a class="header-anchor" href="#stage-2-—-enhance" aria-label="Permalink to &quot;Stage 2 — Enhance&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">yarn</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> enhance</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> v9</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --full</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verbose</span></span></code></pre></div><p>Enriches the raw schema with the configured LLM provider and writes <code>data/v9/fluentui-schema-enhanced.json</code>. Every write is validated; the CLI exits non-zero on any validation error.</p><ul><li><code>--full</code> re-enhances everything (ignores the diff). Without it, enhancement is diff-based against the previous enhanced schema.</li><li>Requires provider env vars (see <a href="/fluentui-mcp/reference/configuration.html">Configuration</a>).</li><li>Model handling is family-aware — see <a href="/fluentui-mcp/decisions/ADR-003-llm-provider-model-family.html">ADR-003</a>.</li></ul><h3 id="whole-pipeline" tabindex="-1">Whole pipeline <a class="header-anchor" href="#whole-pipeline" aria-label="Permalink to &quot;Whole pipeline&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">yarn</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pipeline:full</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># = scrape --clone --verbose → enhance --full --verbose → build → test</span></span></code></pre></div><h2 id="ci-cd" tabindex="-1">CI/CD <a class="header-anchor" href="#ci-cd" aria-label="Permalink to &quot;CI/CD&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Workflow</th><th>Trigger</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ci.yml</code></td><td>push / PR to <code>main</code></td><td>Build &amp; test on Node 20 and 22</td></tr><tr><td><code>publish.yml</code></td><td>manual (<code>workflow_dispatch</code>)</td><td>Build, test, verify package, publish to npm (release is manual)</td></tr><tr><td><code>update-docs.yml</code></td><td>manual (<code>workflow_dispatch</code>)</td><td>Scrape + enhance + build/test, then open a PR with the updated <code>data/</code> schema</td></tr><tr><td><code>deploy-techdocs.yml</code></td><td>push to <code>main</code> (<code>techdocs/**</code>) + manual</td><td>Build this VitePress site and deploy to GitHub Pages</td></tr></tbody></table><h3 id="update-docs-yml-operational-prerequisites" tabindex="-1"><code>update-docs.yml</code> operational prerequisites <a class="header-anchor" href="#update-docs-yml-operational-prerequisites" aria-label="Permalink to &quot;\`update-docs.yml\` operational prerequisites&quot;">​</a></h3><p>The workflow is valid against the current scraper/enhancer CLIs, but it depends on two repo settings that are <strong>not</strong> in code:</p><ol><li><strong>Repository secrets</strong> must be set: <code>LLM_PROVIDER</code>, <code>OPENAI_API_KEY</code> and/or <code>ANTHROPIC_API_KEY</code>, and optionally <code>LLM_MODEL</code>. Without them the enhance step fails at runtime.</li><li><strong>PR-creation permission</strong>: Settings → Actions → General → &quot;Allow GitHub Actions to create and approve pull requests&quot; must be enabled for the <code>peter-evans/create-pull-request</code> step to open its PR.</li></ol><h3 id="publishing-to-npm" tabindex="-1">Publishing to npm <a class="header-anchor" href="#publishing-to-npm" aria-label="Permalink to &quot;Publishing to npm&quot;">​</a></h3><p><code>publish.yml</code> is manual only. Run it from the Actions tab; it defaults to a dry run and only publishes when <code>dry_run=false</code>. It requires the <code>NPM_TOKEN</code> secret. The publish job runs after <code>build-and-test</code> passes.</p><h2 id="adding-a-tool" tabindex="-1">Adding a Tool <a class="header-anchor" href="#adding-a-tool" aria-label="Permalink to &quot;Adding a Tool&quot;">​</a></h2><p>See <a href="/fluentui-mcp/architecture/api-design.html#adding-or-changing-a-tool">API Design → Adding or Changing a Tool</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/development.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const development = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  development as default
};
